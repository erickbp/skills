interface:
  display_name: "Task Splitter"
  short_description: "Decompose specs, verifier gaps, or UAT findings into execution-ready task cards"
  default_prompt: "Use $task-splitter to turn this spec, migration plan, or verification report into fresh-thread-safe task cards with a decision ledger, coverage map, must-haves, and execution waves."
